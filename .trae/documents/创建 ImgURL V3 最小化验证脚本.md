我明白您的意思了。之前的判断有误，根据您提供的官方文档，**ImgURL V3 接口确实是使用 `sk-` 开头的 Token，并通过 `Authorization: Bearer` 头进行验证**。我之前的自动识别逻辑（认为 `sk-` 是 V2）是错误的。

为了避免在主代码中反复试错，我将按照您的要求，先创建一个最小化的独立测试文件来验证接口通断。

### 计划步骤

1.  **创建独立测试脚本 `test_imgurl_v3.html`**
    -   包含 Base URL、Token 输入框（默认预填 `sk-` 格式提示）。
    -   包含文件选择器。
    -   **核心逻辑**：
        -   使用 `POST /api/v3/upload`。
        -   设置 Header: `Authorization: Bearer <token>`。
        -   使用 `FormData` 上传文件。
    -   **调试输出**：直接在页面上显示完整的请求头、请求体摘要和服务器响应 JSON，方便您确认 `invalid.token` 是否消失。

2.  **验证通过后（待确认）**
    -   一旦您通过该测试文件确认上传成功，我将立即修正主程序 `js/api.js` 和 `js/ui.js`。
    -   移除错误的 Token 格式拦截逻辑。
    -   统一使用 V3 接口规范。

我现在将创建这个测试文件，请确认执行。