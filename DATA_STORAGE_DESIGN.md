# 📁 日记应用数据存储方案设计

## 🎯 核心理解

**重要概念**：File System API 将数据保存在**客户端（用户本地电脑）**，而不是服务器。

```
┌─────────────────────────────────────────────────────────┐
│  用户电脑 (客户端)                                       │
│                                                         │
│  📁 C:\Users\YourName\Documents\MyDiary\               │
│     ├─ diary.json        ← 日记数据                    │
│     ├─ weekly.json       ← 周记数据                    │
│     └─ backup/           ← 备份文件夹                  │
│                                                         │
└─────────────────────────────────────────────────────────┘
                    ↕ HTTPS
┌─────────────────────────────────────────────────────────┐
│  Docker 服务器 (172.245.142.4:3000)                     │
│                                                         │
│  📦 diary-app 容器                                      │
│     └─ 静态文件 (HTML/CSS/JS)                          │
│                                                         │
│  ⚠️ 服务器不存储用户数据                                 │
└─────────────────────────────────────────────────────────┘
```

## 🗂️ 推荐的客户端文件组织结构

### 方案 1：简洁版（推荐新手）

```
📁 MyDiary/
├─ diary.json           ← 日记数据（自动生成）
├─ weekly.json          ← 周记数据（自动生成）
└─ backup/              ← 手动备份
   ├─ diary-2026-01.json
   ├─ diary-2026-02.json
   └─ diary-2026-03.json
```

**推荐位置**：
- Windows: `C:\Users\你的用户名\Documents\MyDiary`
- macOS: `/Users/你的用户名/Documents/MyDiary`
- Linux: `/home/你的用户名/Documents/MyDiary`

### 方案 2：专业版（推荐高级用户）

```
📁 DiaryCloud/                    ← 云同步根目录
├─ data/                          ← 原始数据
│  ├─ diary.json                 ← 日记
│  ├─ weekly.json                ← 周记
│  └─ config.json                ← 配置（未来）
│
├─ backup/                        ← 定期备份
│  ├─ daily/                     ← 每日备份
│  ├─ weekly/                    ← 每周备份
│  └─ monthly/                   ← 每月备份
│
├─ export/                        ← 导出文件
│  ├─ diary-backup-202601.json
│  └─ diary-report-202601.pdf    ← 未来功能
│
└─ archive/                       ← 归档（年度）
   ├─ 2024/
   ├─ 2025/
   └─ 2026/
```

**推荐位置（使用云盘同步）**：
- **坚果云**: `C:\Users\你的用户名\Nutstore\MyDiary`
- **Dropbox**: `C:\Users\你的用户名\Dropbox\MyDiary`
- **OneDrive**: `C:\Users\你的用户名\OneDrive\MyDiary`
- **百度网盘**: `C:\Users\你的用户名\baiduYun\MyDiary`

### 方案 3：开发版（适合你）

```bash
# WSL 环境下的专业结构
📁 /home/aa/DiaryApp/
├─ data/                         ← 数据文件
│  ├─ diary.json
│  └─ weekly.json
│
├─ backup/                       ← 备份
│  └── .gitkeep
│
├─ export/                       ← 导出
│  └── .gitkeep
│
└── README.md                    ← 说明文档
```

## 🚀 部署后的存储引导

### 在应用中添加存储引导提示

当用户首次访问时，显示引导：

```
┌─────────────────────────────────────────────────┐
│  欢迎使用智能日记本！                             │
│                                                 │
│  📁 请选择日记保存位置：                          │
│                                                 │
│  推荐选择：                                      │
│  ☐ 使用云同步文件夹（坚果云/Dropbox等）          │
│  ☐ 使用本地文档文件夹                            │
│  ☐ 自定义位置                                    │
│                                                 │
│  💡 提示：                                         │
│  - 选择一个固定的文件夹                          │
│  - 建议使用云盘实现多设备同步                    │
│  - 定期备份 diary.json 文件                     │
│                                                 │
│  [开始使用]                                      │
└─────────────────────────────────────────────────┘
```

## 📋 部署检查清单

### 服务器端（Docker）

```bash
# ✅ 服务器上不需要特殊的目录结构
# 因为数据保存在客户端

# 但可以添加README提醒
cat > README.md << 'EOF'
# Diary App Deployment

## 注意
此应用使用 File System API，用户数据保存在客户端浏览器，
不需要在服务器上创建数据目录。

## 端口
- HTTP: 3000

## 更新
docker stop diary-app && docker rm diary-app
docker load < diary-app.tar
docker run -d -p 3000:3000 --name diary-app diary-app:latest
EOF
```

### 客户端（用户）

给用户的使用建议：

**Windows 用户**:
```
创建文件夹: C:\Users\你的用户名\Documents\MyDiary
在该文件夹中保存 diary.json
```

**macOS 用户**:
```
创建文件夹: ~/Documents/MyDiary
在该文件夹中保存 diary.json
```

**Linux/WSL 用户**:
```
mkdir -p ~/MyDiary
cd ~/MyDiary
# 在浏览器中保存 diary.json 到这个目录
```

## 🔄 数据备份策略

### 自动备份（推荐）

创建一个简单的备份脚本：

```bash
#!/bin/bash
# backup-diary.sh

DIARY_DIR="$HOME/MyDiary"
BACKUP_DIR="$DIARY_DIR/backup"
TIMESTAMP=$(date +%Y%m%d_%H%M%S)

mkdir -p "$BACKUP_DIR"

# 备份 diary.json
if [ -f "$DIARY_DIR/diary.json" ]; then
    cp "$DIARY_DIR/diary.json" "$BACKUP_DIR/diary-$TIMESTAMP.json"
    echo "✅ 备份成功: diary-$TIMESTAMP.json"
fi

# 保留最近 30 天的备份
find "$BACKUP_DIR" -name "diary-*.json" -mtime +30 -delete

# 可选：提交到 Git
cd "$DIARY_DIR"
git add --all
git commit -m "Auto backup: $TIMESTAMP"
```

### 云同步方案

**方案 1: 使用坚果云（推荐中国用户）**
```
1. 安装坚果云客户端
2. 同步文件夹设置为: C:\Users\你的用户名\Nutstore\MyDiary
3. 在应用中选择这个文件夹保存 diary.json
4. 自动同步到所有设备
```

**方案 2: 使用 Git + GitHub 私有仓库**
```bash
cd ~/MyDiary
git init
git add diary.json weekly.json
git commit -m "Initial commit"
git remote add origin https://github.com/your-username/my-diary.git
git push -u origin main
```

## 🎯 你的推荐方案

基于你的 WSL 环境，我推荐：

```bash
# 1. 创建数据目录
mkdir -p ~/MyDiary/{backup,export}

# 2. 创建说明文件
cat > ~/MyDiary/README.md << 'EOF'
# 我的日记

## 数据文件
- `diary.json` - 日记数据
- `weekly.json` - 周记数据

## 备份
定期 backup/ 文件夹

## 同步
建议使用云盘同步此文件夹
EOF

# 3. 添加到 .bashrc 便捷命令
cat >> ~/.bashrc << 'EOF'

# Diary App 快捷命令
alias diary-backup='cp ~/MyDiary/*.json ~/MyDiary/backup/'
alias diary-ls='ls -lh ~/MyDiary/*.json'
alias diary-cd='cd ~/MyDiary'
EOF

# 4. 重新加载配置
source ~/.bashrc
```

## 📊 部署文档更新

更新你的部署记录，添加存储说明：

```markdown
## 用户数据存储说明

此应用使用 File System API，数据保存在用户本地：

1. 用户首次使用时选择保存位置
2. 推荐保存到：
   - Windows: C:\Users\用户名\Documents\MyDiary
   - macOS: ~/Documents/MyDiary
   - Linux: ~/Documents/MyDiary

3. 备份建议：
   - 定期复制 diary.json 文件
   - 使用云盘同步整个文件夹
   - 或使用 Git 进行版本控制

4. 多设备使用：
   - 将 diary.json 保存在云同步文件夹
   - 在不同设备上访问同一文件
   - 注意同时编辑的冲突问题
```

这样设计清楚吗？需要我帮你创建具体的初始化脚本吗？
