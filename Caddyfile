doing-dairy.wdhonghong.sbs {
    encode gzip zstd

    log {
        output stdout
        format console
    }

    # ⚠️ 不确定因素: 当前按上游 diary-app:4000 配置，若服务名或端口调整需同步修改。
    reverse_proxy diary-app:4000
}
